  <%= form_for @opinion, url: new_opinion_path, remote: true do |f| %>
	  <%= f.text_area :Text, class: "form-control", placeholder: "review here" %>
	  <%= f.submit "Speak!", class: "btn-primary btn-sm mt-2" %>
  <% end %>
